const express = require('express');
const router = express.Router();

// Implémentez vos routes client ici
// Exemple :
// router.get('/', clientController.getAllClients);
// router.get('/:id', clientController.getClientById);
// etc.

module.exports = router;
